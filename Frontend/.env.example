# Backend API URL (no trailing slash).
# Local:
# VITE_API_URL=http://localhost:3000
# Production (Vercel env var — must be set at BUILD time):
# VITE_API_URL=https://hirepilotai-whej.onrender.com
VITE_API_URL=http://localhost:3000
